name: Manual Bisect Android Build

on:
  workflow_dispatch:
    inputs:
      commit_hash:
        description: 'The full or short Git commit SHA to build'
        required: true
        default: 'HEAD'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4
        with:
          # Fetch all history so GitHub can find your old commit
          fetch-depth: 0 

      - name: Checkout Specific Commit
        run: |
          git checkout ${{ github.event.inputs.commit_hash }}

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Build APK via Gradle
        run: |
          # Changed to target the correct nested 'phoenix' directory
          cd pkg/android/phoenix
          chmod +x gradlew
          ./gradlew assembleNormalRelease

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: retroarch-bisect-${{ github.event.inputs.commit_hash }}
          # Path updated to look inside the phoenix directory hierarchy
          path: pkg/android/phoenix/pkg/build/outputs/apk/normal/release/*.apk
